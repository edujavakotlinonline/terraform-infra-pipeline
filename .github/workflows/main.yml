name: "PROD DEPLOY"

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    uses: ./.github/workflows/terraform.yml
    with:
      environment: prod
      aws-assume-role-arn: "arn:aws:iam::115825878896:role/github-actions-edujavakotlinonline-curso-inicial-terraform"
      aws-region: "sa-east-1"
      aws-statefile-s3-bucket: "edujavakotlinonline-sa-east-1-terraform-statefile"
      aws-lock-dynamodb-table: "edujavakotlinonline-sa-east-1-terraform-lock"